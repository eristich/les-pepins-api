nelmio_api_doc:
    documentation:
        info:
            title: Les Pepins API
            description: Une API pour le questionnaire du dashboard prédictif Les Pepins
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
